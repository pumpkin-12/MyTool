# Tauri / Rust 构建产物
src-tauri/target/
src-tauri/gen/schemas/

# Node（仅在需要 tauri CLI 时才会出现）
node_modules/
package-lock.json

# 临时探测 / 命令输出抓取
_*.txt
.workbuddy/_*.txt

# 系统
Thumbs.db
desktop.ini
.DS_Store

# 应用数据（用户指定存项目目录，不入仓）
data/
