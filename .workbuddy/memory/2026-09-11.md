# 2026-09-11

## 项目巡检：个人工具箱（首次盘点）

- 项目形态：单文件应用 `D:\Ai-file\MyTool\index.html`，3349 行 / 147 KB，最后修改 2026-09-11 08:57。项目目录下**只有这一个文件**。
- 分层结构（自上而下）：
  1. `AppStore` 存储抽象层 —— 三后端自动探测：Tauri `invoke('store_read'/'store_write')` / Electron `window.NativeStore.readAll/writeAll` / 浏览器本地存储。`ready` 是 Promise，界面初始化必须等它就绪（有 3 秒超时兜底）。
  2. 路由与主题 —— hash 路由 `#tool/<id>`，`renderHome()` / `renderTool()`；浅色深色切换（偏好存 `theme`），自研左栏行号+高亮滚动同步。
  3. 工具注册表 —— `registerTool()` 入 `registry[]`，`ENABLED_TOOLS` 白名单控制首页显示，首页卡片可拖拽排序（存 `home:order`）。
  4. 三个工具：`internlog`（实习日志 · 贡献度热力图 · 标签 · 报表导出）、`sketch`（无限画板 · 9 种工具 · undo/redo 50 步 · 多选复制粘贴 · 层级 · 右键菜单 · PNG/SVG/JSON 导出 · 迷你地图 · 对齐参考线 · 图片插入 · 触屏缩放）、`mermaid`（编辑器 · CDN+本地双加载 · 自研语法高亮 · 防抖渲染+竞态丢弃 · 错误定位到行 · 历史版本 · 缩放导出）。
- 数据 key 命名约定：`toolbox:<tool>:<field>`，如 `internlog:items`、`sketch:*`、`mermaid:doc` / `mermaid:hist`、`theme`、`home:order`。

## 发现的隐患（未处理，待用户决定）

1. **`vendor/mermaid.min.js` 缺失** —— 代码里 `LOCAL = 'vendor/mermaid.min.js'` 用于桌面端离线加载，但项目里没有 `vendor/` 目录，目前实际只能走 CDN。迁移 Electron/Tauri 前必须补上，否则断网即白屏。
2. **无版本控制、无 README、无 `.workbuddy`** —— 147 KB 单文件改动风险高，建议 `git init` 后再动大功能。
3. 顶部 6 处 `data-page-node-id` 属性是某个页面编辑器留下的残留标记，可清理。

## 环境备注

- 本机 Git Bash 环境异常：`dirname` / `ls` / `wc` 均 not found（exit 127），Bash 工具基本不可用；本轮改用 PowerShell + Glob/Read/Grep。
- PowerShell 工具 stdout 未被回传（exit 0 但无输出），需要输出时改为「写文件 → Read 读回」的方式绕过。

## 产出：Tauri 桌面端迁移方案

交付物：`docs/tauri-迁移方案.html`（自包含 HTML，浅色主题，可打印）。

核心结论：**不是重写**。现有代码已预埋 Tauri 分支（`AppStore` 三后端探测 + `window.__TAURI__.core.invoke` + `isDesktop` 分流 Mermaid 加载），缺的只是 Rust 侧工程与配置。

### 技术选型（核准于 2026-09-11）
- 用 Tauri **2 稳定线**：tauri crate 2.11.5 / tauri-cli 2.11.4 / @tauri-apps/api 2.11.1。
- **不用 Tauri 3**：CEF 后端仍是 early alpha（tauri-cef-v3.0.0-alpha.22），对只面向 Windows 的项目零收益。
- 不引入 Vite/打包器，保持单文件零构建。

### 四个阻塞项（已写进方案）
- **B1** `vendor/mermaid.min.js` 缺失 → 需补 mermaid 11 的 **UMD** 构建（`viaLocal()` 读 `window.mermaid` 全局；CDN 那路才是 ESM）。
- **B2** Rust 侧 `store_read` / `store_write` 不存在。
- **B3** 必须 `app.withGlobalTauri: true`，否则 `window.__TAURI__` 不存在 → 永远走 localStorage → 数据存 WebView 缓存里随时丢。
- **B4** 必须 `app.windows[].dragDropEnabled: false`。

### 高危项
- **H1** 5 处导出走 `<a download>` + blob/data URL，WebView2 下行为不可控 → 改原生 save_file。
- **H2** `persist()` 每次 set 全量写盘且无串行化 → invoke 完成顺序无保证，有后写覆盖先写的竞态。
- **H3** native 分支 `AppStore.set` 恒返回 true → 画板「保存失败」提示永不触发。
- **H4** 画板图片 base64 内联进 `shapes`，随 JSON 全量 stringify。
- **H5** 浏览器 → 桌面端无数据迁移通道（仅画板有 exportJSON）。

### 关键技术坑（已用官方文档核实）
1. **`dragDropEnabled` 与 HTML5 拖放互斥**——Windows 上 Tauri 用原生 OLE 拖放目标，会**吞掉** WebView 的 `drop` 事件（表现为禁止光标，`dragstart` 有但 `dragover`/`drop` 不触发）。macOS/Linux 无此问题。本项目的**首页卡片拖拽排序**和**画板拖入图片**都依赖 HTML5 DnD，所以必须设 `false`。且该属性只在 `tauri.conf.json` 声明窗口时可靠生效，写在 Rust window builder 里会被静默忽略。
2. **IPC 传二进制不要用 `Vec<u8>`**——serde_json 会把每个字节展开成 `255,` 形式，5MB PNG 的 payload 可膨胀到十几 MB。改用 **base64 字符串 + Rust `base64` crate 解码**。
3. **IPC 参数命名**：前端 camelCase ↔ Rust snake_case 自动转换，两头都写 snake_case 反而报缺参数。
4. **权限系统只管插件/核心命令**——自定义命令（`generate_handler!` 注册的）不受 capability 管辖，`core:default` 就够，不必给自定义命令加权限。
5. `csp: null` 先不收紧：index.html 有巨型内联 `<script>` 和大量内联 style，收紧需 `'unsafe-inline'` + 放行 `cdn.jsdelivr.net`，属独立任务。
6. `productName` 用 ASCII（NSIS 输出文件名会用到），中文显示用窗口 `title`。
7. `tauri-plugin-dialog` 的 `FilePath::into_path(self) -> Result<PathBuf, Error>`；`blocking_save_file()` 不能在主线程调用 → 命令声明为 `async fn`。

### 待用户拍板
1. `bundle.identifier`（硬阻塞，决定数据目录 `%APPDATA%\<id>\`，定了不能改）
2. `index.html` 是否移入 `web/`（建议移入，`frontendDist: "../web"`）
3. 画板图片是否外置为文件（第 2 档，可选）
4. 是否额外产出便携版

### 后续
- 动工前先 `git init` 并提交现状（无版本控制 + 147KB 单文件，风险高）。
- 建议第 1 档顺手加「存档轮转保留最近 3 份备份」——单点故障，画板与实习日志都是不可再生数据。
- **迁移全流程实测通过后，再把它沉淀成 skill**（现在只到方案阶段，未实测的步骤不宜入库）。

## 实施：Tauri 桌面端迁移（第 0 / 1 档已落地）

提交：`5349f68` 迁移前基线 → `43d743c` 迁移改动（28 files changed）。
用户决策「按你建议的来」→ 采用推荐值：
- `bundle.identifier` = **`com.lijiazhen.toolbox`**（由 git config user.name=lijiazhen 推得）。数据目录 `%APPDATA%\com.lijiazhen.toolbox\`，**定了不能改**。
- `index.html` → **`web/index.html`**，`frontendDist: "../web"`（git 识别为 rename，94% 相似）。
- 画板图片外置：本轮不做（第 2 档）。
- 打包：NSIS；便携版 = `src-tauri/target/release/toolbox.exe`。

### 最终目录结构
```
MyTool/
├─ web/index.html              前端（原 index.html）
├─ web/vendor/mermaid.min.js   3.5 MB / mermaid 11，离线用
├─ src-tauri/  Cargo.toml, Cargo.lock, build.rs, tauri.conf.json,
│              capabilities/default.json, src/{main,lib}.rs, icons/（16 个）
├─ tools/  make-icons.py, verify-appstore.js
├─ docs/tauri-迁移方案.html
└─ 实习日志备份-2026-09-11.json   用户迁移前自己导出的数据
```

### 关键技术产出与实测结论
1. **mermaid 离线文件**：`https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js`（3,572,661 字节）。
   - 它**不是 UMD**，是 esbuild 的 IIFE，但**最后一行**是 `globalThis["mermaid"] = globalThis.__esbuild_esm_mermaid_nm["mermaid"].default;` → 会挂到 `window.mermaid`，所以 **`viaLocal()` 一行都不用改**。
   - `chunkRefs=0`、`dynamicImports=0`，单文件自包含，无懒加载拆分。
   - **教训**：不要靠 grep 字面量 `window.mermaid` 判断——它用的是 `globalThis.mermaid`。要点是**实际在 Node 的 `vm` 里跑一遍**看全局对象上有什么（`tools/` 下没有留这个探针，方法记在这里）。
2. **Rust 侧三条命令**（`src-tauri/src/lib.rs`）：`store_read` / `store_write` / `save_file`。
   - `store_write`：先 `serde_json::from_str` 校验合法性 → 轮转备份 → 临时文件 + `fs::rename` 原子替换。
   - **额外加固（方案外）**：存档轮转 `store.json.1/.2/.3`，**30 分钟节流**（`Mutex<Option<Instant>>` 放 Tauri state）。不节流的话画板 400ms 防抖会反复复制几 MB 文件。
   - `save_file`：**async fn**（`blocking_save_file()` 不能跑主线程）；收 **base64 字符串**而非 `Vec<u8>`（字节数组走 JSON IPC 会把每个字节展开成 `255,`，体积爆炸）；用 `base64::engine::general_purpose::STANDARD`。
   - IPC 参数命名：前端 camelCase ↔ Rust snake_case **自动转换**，两头都写 snake_case 会报缺参数。
3. **前端补丁 A（H2/H3）**：`AppStore.persist()` 改 Promise 链串行化；新增 `flush()` / `lastError()`；`set()` 返回值从恒定 true 改为真实写盘结果（有一次性延迟）。
4. **前端补丁 B（H1）**：新增全局 `saveBlob()` / `blobToBase64()`，5 处导出全改道原生另存为；删掉 `downloadBlob()` 与 `download(url,name)`。画板 `exportPNG` 从 `toDataURL` 改 `toBlob`。
5. **H5 修正**：`internlog`（line 931）和画板（line 2379）**本来就有导入功能**，真正缺导入的只有 Mermaid。所以改了思路——首页加「导出全部数据 / 导入全部数据」一次覆盖三个工具，而不是给 Mermaid 单独补 UI。`AppStore.exportAll()/importAll()` 用 `PREFIX = 'toolbox:'` 枚举/写回，两端格式对称。

### 验证状态（重要：Rust 侧尚未过编译器）
- **前端已实测通过**：`tools/verify-appstore.js` 用 Node `vm` 把 AppStore 源码抠出来跑两种模式，**31 passed / 0 failed**。覆盖：内联脚本语法、浏览器模式 CRUD、原生模式 `store_read`、写盘快照递增（证明无后写覆盖先写）、写盘失败弹提示 + `lastError()`、`set()` 正确返回 false、全量导出导入往返。
- **Rust 侧未编译验证**：`cargo check` 冷启动要拉 ~500 个 crate，跑了 10 分钟仍未完，**被用户取消**（用户要自己手动编译）。`src-tauri/Cargo.lock` 已生成并提交（说明依赖解析成功）。
- **运行 `tools/verify-appstore.js`**：`node tools/verify-appstore.js web/index.html <报告路径>`。

### 环境坑（本机）
- **Bash 工具报 `dirname`/`ls`/`wc` not found（exit 127）—— 这是可修的，别浪费时间去绕。**
  **修正**：`~/.workbuddy/skills/agent-browser-windows-verify/SKILL.md` 里早就记录了根因和修法——Git Bash 的默认 PATH 缺 coreutils。前置 `$GIT_USR` 即可拿回来：
  ```bash
  NODE_DIR=/c/Users/DELL/.workbuddy/binaries/node/versions/22.22.2-3
  GIT_USR=/c/Users/DELL/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin
  export PATH="$NODE_DIR:$GIT_USR:/c/Windows/System32:/c/Windows"
  ```
  本次全程改用 PowerShell 属于**走了弯路**；下次先读这个 skill 再动手。
- **PowerShell 工具 stdout 不回传**：exit 0 但无输出。**必须「命令写文件 → Read 读回」**。
- **PowerShell 直接输出中文会 mojibake**（git/Node 输出按 GBK 解码）。要么让子进程自己写 UTF-8 文件，要么用 ASCII 标签。
- **`Remove-Item` 被 safe-delete shim 拦截**（`trash-failed` → fail-closed 拒绝硬删）。清理临时文件要用 `[System.IO.File]::Delete($path)`。
- 托管 Python：`C:\Users\DELL\.workbuddy\binaries\python\envs\default\Scripts\python.exe`，**Pillow 12.3.0 可用**。
- 托管 Node：`C:\Users\DELL\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`。
- git 全局身份：`lijiazhen` / `lijiazhen0926@163.com`。

## 编译成功 + 运行时验证结果（用户手动编译）

用户自行编译通过（debug 构建：`src-tauri/target/debug/toolbox.exe`，12.84 MB）。**我的 Rust 代码未经我这边编译器验证就直接通过了**，说明 `lib.rs` 里三条命令的 API 用法正确。

去磁盘核对了端到端链路，`%APPDATA%\com.lijiazhen.toolbox\store.json`（1598 字节）：

| 存档顶层 key | 说明 |
|---|---|
| `toolbox:home:order` | 值 = `sketch > mermaid > internlog` —— **用户拖动过卡片排序** |
| `toolbox:internlog:items` | 4 条，与迁移前导出的 `实习日志备份-2026-09-11.json` 的 4 条吻合 |

**由此运行时确认**：
- ✅ B2/B3 解决：`withGlobalTauri` 生效 → `__TAURI__` 被探测到 → 数据落在**文件**而非 localStorage
- ✅ B4 解决：`dragDropEnabled: false` 生效 —— **HTML5 拖放（卡片拖拽排序）真的工作了**
- ✅ 实习日志导入功能可用，写入落盘正常
- ✅ 存档前缀格式 `toolbox:<tool>:<field>` 在原生模式下保持一致

**尚未验证**（下次继续）：
- ❓ `save_file` 原生另存为对话框（最没把握的一条路径：invoke 参数命名 + base64 解码 + 对话框）
- ❓ Mermaid 离线加载（需断网打开编辑器验证 `vendor/mermaid.min.js`）
- ❓ 画板（存档里还没有任何 `toolbox:sketch:*` 键，说明还没用过）
- ❓ 主题切换（无 `toolbox:theme`）
- ❓ release 构建 / NSIS 安装包（目前只有 debug 版）

## 沉淀
新建 skill `~/.workbuddy/skills/tauri2-desktop-wrap/SKILL.md` —— 把零构建单文件 Web 应用封装成 Tauri 2 桌面端的完整流程，含 4 个必设配置、dragDropEnabled/Windows 硬坑、base64 IPC、写盘串行化、Node vm 双模式断言验证法。

## 出包完成（用户手动 `tauri build`）

产物（`src-tauri/target/release/`）：
- **安装包** `bundle/nsis/PersonalToolbox_0.1.0_x64-setup.exe` —— **1.92 MB**
- **便携版** `toolbox.exe` —— **3.93 MB**

NSIS 工具链已缓存到 `%LOCALAPPDATA%\tauri\NSIS`（首次构建时联网下载的）。
**Tauri 的体积承诺是真的**：一个带三个工具的桌面应用，安装包不到 2 MB。

### 从存档反推的运行时验证进度
`store.json` 顶层 key：`home:order`, `internlog:items`, `sketch:canvas`, `mermaid:doc`, `mermaid:hist`, `theme`

- ✅ **主题切换可用**（有 `toolbox:theme`）
- ✅ **Mermaid 库加载成功**。关键推理：`doRender()` 里 `loadLib()` 失败会**提前 return 且不调用 `scheduleSave`**，所以 `mermaid:hist` 存在 ⇒ 库一定加载成功了。又因为桌面端加载顺序是 `[viaLocal, viaCdn]` 且 `web/vendor/mermaid.min.js` 确实存在，**走本地那条路的可能性极大**（但严格证明仍需断网重开一次）。
- ⚠️ **画板只是被打开过，没画东西**。`toolbox:sketch:canvas` 是 `index.html:2718` 的 `AppStore.set(DB + 'canvas', '')`，注释写着「清理旧版像素画布的残留数据」——这是个**遗留清理键，不代表用户内容**。没有 `sketch:shapes` 说明还没编辑过。
- ✅ **存档轮转备份生效**：`store.json.1`（1598 字节）就是迁移后的首版存档。

### 尚未验证
- ❓ `save_file` 原生另存为对话框——唯一还没被碰过的路径。

## ⚠️ 我漏做的一件事：单实例
方案第 1 档里我列了「单实例（第二次启动聚焦已有窗口）」，但**实现时漏了**，`lib.rs` 里没有 `tauri-plugin-single-instance`。

后果是真实的：`AppStore` 启动时把**整个存档读进内存**，之后每次 `set` 都全量写回。所以开两个实例（比如装好的版本 + 便携版同时开着）会互相覆盖——后写的那份把另一个实例的所有改动抹掉。这是**数据丢失风险**，不是体验问题。

修法很小（约 5 行）：

```toml
# Cargo.toml
tauri-plugin-single-instance = "2"
```
```rust
// lib.rs run() 里，必须放在最前面
.plugin(tauri_plugin_single_instance::init(|app, _argv, _cwd| {
    if let Some(w) = app.get_webview_window("main") {
        let _ = w.unminimize();
        let _ = w.set_focus();
    }
}))
```

注意顺序：`.plugin(tauri_plugin_single_instance::init(...))` 要在 `.plugin(tauri_plugin_dialog::init())` **之前**注册才可靠。

## 产出：quiz-app 集成方案

交付物：`docs/quiz-app-集成方案.html`。

### quiz-app 实况（2026-09-11 盘点）
```
D:\Ai-file\quiz-app\
├─ backend/    Spring Boot 2.7.18 / Java 11 / Maven
│              target/quiz-backend-1.0.0.jar = 43.39 MB（已含前端 static/）
└─ frontend/   Vue 2.7 + Vue Router 3 + Vuex 3 + Element UI 2.15 + axios + xlsx + file-saver
               dist 不含 sourcemap = 1.89 MB（含 map 8.73 MB）
```
- **数据结构**：dev = `jdbc:h2:mem:quizdb`（**内存库，重启即丢**，配置文件注释自己都写了）；prod = MySQL。
  → **无论选哪个集成方案，第一步都得先让它持久化。**
- **5 个 Controller / 约 19 个 REST 端点**：`/api/questions`(8) · `/api/quiz`(7) · `/api/wrong-questions`(3) · `/api/stats`(1) · `/api/records`(3)。响应体统一 `{code, message, data}`。
- **前端只有一个 API 收口**：`frontend/src/api/request.js`（axios 实例，`baseURL:'/api'`，响应拦截器解包 `res.data`）。换后端只要替这一个文件的导出。
- **Vue Router 是 hash 模式**（`new VueRouter({routes})` 没设 mode）→ 静态文件直接能跑、不需要服务器重写，放 iframe 里也不会和工具箱的 `#tool/<id>` 冲突。
- **Excel 导入前端做**：`importQuestions(questions)` 传的是解析好的数组，`xlsx` 在浏览器里解析，后端不碰 Excel。
- 本机：Java 17 Temurin + javac + **jpackage** + Maven 3.9.16 都在；`JAVA_HOME` 为空。
- git 只有 2 个提交、**48 项未提交改动**（活跃开发中）。

### 四个方案与推荐
| 方案 | 体积 | 数据在哪 | 能否整体导出 |
|---|---|---|---|
| A Sidecar（Tauri externalBin 搬整套） | → **≈95 MB** | H2/MySQL | 否 |
| B 外置后端 + iframe 入口 | 不变 | H2/MySQL | 否 |
| **C 保留 Vue UI，数据层本地化** | → **≈5.8 MB** | **工具箱存档** | **是** |
| D 纯前端重写 | → ≈4.0 MB | 工具箱存档 | 是 |

**推荐 C**：UI 工作量全保住（BankPanel 18 KB / QuizPanel 12 KB 是真实投入）；Java 逻辑（QuizService 15.9 + QuestionService 5.5 + StatsService 4 + WrongQuestionService 1.9 ≈ 28 KB）翻译成 JS 是一次性成本，且 **Java 版留着当规格说明**，`QuizServiceTest.java`(31 KB) 直接就是验收用例；体积只 +1.89 MB，和已接受的 mermaid 3.5 MB 同量级。

**一个要用户点头的破例**：工具箱硬约定是「不引入打包器」，方案 C 会让仓库出现 Vue 构建产物。建议折中——源码留 quiz-app 仓库，工具箱只收 dist 当 vendor 资源（和 `mermaid.min.js` 同级），不在工具箱里跑 npm。

### 已知要提前处理的技术点
- **存档体量**：工具箱每次保存都全量 stringify 整个存档。题库上千题会长得很快，**建议 quiz 数据拆独立文件**——正好和工具箱第 2 档「存档按 key 拆分」合并做。
- **iframe 与父页同源**（都是 `tauri://localhost`）→ **共享 localStorage**。本地适配层要么统一走 AppStore，要么加独立 key 前缀，否则污染 `toolbox:*` 体系。
- 构建时关掉 `productionSourceMap`，否则多拷 6.8 MB 无用文件。
- 保留 `request.js` 的 axios 分支，这样 quiz-app 仍能前后端分离跑 `npm run serve` 用浏览器调，两边共用一个界面代码库。

### ⚠️ 同日修正：推荐从 C 改为 **D（重写）**
我最初推荐 C（保留 Vue UI + 数据层本地化），理由是"UI 工作量全部保住、重写是纯浪费"。**逐个文件读过之后，这个判断是错的。** 已就地更正 `docs/quiz-app-集成方案.html`（第 01 节加修正提示、第 04 节标注已被修正、新增第 08 节完整依据）。

**推翻原判断的三个事实：**

1. **后端要搬的不是 28 KB Java，而是约 185–205 行真实规则。**
   按实义行数（去掉 import/空行/注解/注释）统计：QuizService 291、QuestionService 99、StatsService 68、WrongQuestionService 30，合计 488 行；其中真正的领域规则约 185–205 行。剩下的全是**五参数构造器注入、DTO↔实体映射、Repository 调用、`JsonProcessingException` 包装、响应对象逐字段赋值**——这些只因为"前后端分离 + ORM"才存在，本地单用户应用里没有网络边界，全部消失。
   **重写最容易漏的规则**：`normalizeTrueFalseAnswer`（`对/正确/TRUE/T` → `A`，其余算 `B`，带历史数据兼容）；判分**分母只算可判题**（choice+truefalse，填空/简答不拉低正确率）；错题本**有两处同步时机**（单题提交 + 交卷时把未答的可判题也补进去）。
   **意外发现**：后端自己就在用「JSON 塞进 TEXT 列」——`session.questionIds`、`session.answers`、`record.answerDetails` 都是 Jackson 序列化后存的。**这个应用的数据模型本来就是 JSON**，搬进 AppStore 不是迁移而是回归自然形态。

2. **前端那 1.8 万字节大半是 Element UI 声明式胶水。**
   BankPanel 418 行 = template 156 + script 260，其中 **62 行 template 含 `el-` 组件标签（占 template 40%）**。真正的领域 UI 只有四件事：按题型分 4 个 Tab、一个工具栏、一张表（只有选择题多 A/B/C/D 四列，靠 `v-if="tab.key==='choice'"`）、两个弹窗。QuizPanel 325 行里 template 只 92 行。

3. **唯一的硬依赖 Excel，成本比我以为的低。**
   `node_modules/xlsx/dist/xlsx.full.min.js` = **861 KB**，可直接 vendor（对比 mermaid 已占 3.5 MB）。**不必退化成 CSV**，用户"用 Excel 出题库"的工作流完整保留。

**方案 C 的真正问题是长期悬置**：收益（复用 UI）是眼前的，代价（仓库多一套 Vue + 构建链、和"零构建单文件"硬约定长期冲突）是持续的；而且走 C 之后迁移到 D 的动因会一直存在却永不被排期（因为"已经能用了"）。**D 是唯一不破坏零构建约定的方案。**

**修正后路线**：第 0 档在 quiz-app 里把领域逻辑抽成 `frontend/src/core/`（与框架无关的纯 JS），把 `QuizServiceTest.java`(31 KB) 的用例翻成 Node 断言跑绿 → **逻辑正确性在搬运前就被单测锁死**，之后界面重写纯体力活；第 1 档搬进工具箱配 vanilla 界面，数据落 `toolbox:quiz:`。
规模估计：逻辑 250–350 行 + 界面 600–900 行 + Excel 封装 100–150 行 ≈ **1000–1400 行**（画板现在约 1400 行）。
代价：`index.html` 从约 3400 行涨到 4500+ 行，"单文件"开始变成负担 → 建议同时把 quiz 数据拆独立存档。

**教训**：评估"重写 vs 复用"时，不要按**文件体积/字节数**下结论。Spring Boot 里大量代码是框架仪式，Vue SFC 里大量是组件库胶水——**必须按"去掉框架痕迹后的实义行数"来估**。这次我按 KB 数猜，直接猜反了。

## 实施：刷题工具（方案 D）已落地

提交 `7a13a7b`，3 个文件，+1479 行。

### 交付内容
- **`web/index.html` 213 KB / 4678 行**（原 147 KB / 3349 行，+1331 行，与预估的 1000–1400 行吻合）
- 新增顶层 **`QuizCore`** IIFE —— 领域逻辑与界面完全解耦，可被 Node 单测抽取
- 新增 `registerTool({ id:'quiz' })`，`ENABLED_TOOLS` 加入 `'quiz'`
- `web/vendor/xlsx.full.min.js`（861 KB，从 quiz-app 的 node_modules 拷贝，**懒加载**——首次用到 Excel 才注入 script）
- `tools/verify-quizcore.js` —— QuizCore 的断言套件

### QuizCore 移植的规则（逐条对齐 Java）
- `normalizeQuestion`：校验顺序与 Java `applyRequest` 一致（题目非空 → 答案非空 → 题型合法 → 按题型归一化）；choice 答案转大写；fill/essay 保原样
- **两个版本的判断题归一化**：写入严格（认不出报错，且「答案不能为空」的检查**先于**归一化，所以空答案报的是「答案不能为空」不是「判断题答案不能为空」——这个测试我自己一开始都写错了）；读取宽松（认不出一律 B）
- `startQuiz`：分类过滤 → 题型过滤（先校验合法）→ 四模式。**exam 不写 examCount**（Java 只在 custom 分支写）；custom 的 `maxTime<=0` 归一为 null；count 一律 `max(1, min(n, size))` 夹紧
- `grade`：分母只算可判题（choice+truefalse）；answers 比题目短时缺答视为 null；题目缺失不判分
- `applyWrongOnAnswer`：答对移除、答错计数+1；**返回新数组不做就地修改**（保证 AppStore.set 能感知变化）
- `syncWrongOnSubmit`：只**新增**缺失的答错/未答可判题，不改已有计数（与 Java `if (!exists) save(...)` 一致）
- `stats`：accuracyRate 保留两位小数；judgedCount 缺失时回退 totalQuestions；未定义模式不进 modeStats；`wrong`（错题重做）模式在 MODE_ORDER 里
- `categories()` 用**码点排序**而不是 localeCompare('zh')——拼音序会把「第二章」排在「第一章」前，码点序对「第N章」恰好自然，也与原数据库 ORDER BY 一致

### 验证
- `tools/verify-quizcore.js`：**85 passed / 0 failed**
- `tools/verify-appstore.js`：31 passed / 0 failed（顺带确认内联脚本语法合法）
- **界面（UI）尚未在真实应用里点过** —— 只验证了逻辑层。这是下一步必须人工做的事。

### 界面要点（vanilla，未用任何框架）
- 六个内页签：仪表盘 / 答题 / 题库 / 错题本 / 记录 / 统计
- 答题：A/B/C/D 选项点击后**锁定**并即时反馈对错 + 解析；填空/简答用 textarea 不判分；答题卡格点；上一题/下一题/提交
- 键盘：A/B/C/D 选择、←/→ 切题、Enter 下一题或提交（INPUT/TEXTAREA/SELECT 聚焦时不劫持）
- 模拟考试倒计时到 0 自动交卷
- 进度保存/恢复（`quiz:progress`，单份，仪表盘可见可放弃）
- Excel：按题型 Tab 决定列映射，逐行 `normalizeQuestion`，失败行计数并报第一处错误；模板按 使用说明.md 的列定义生成
- 弹窗（添加/编辑题目）用 `position:fixed` 临时节点，保存成功后移除

### 用户接下来要做
1. `npx -y @tauri-apps/cli@2 dev` 或直接用已有安装包，进「刷题」实测：抽题、判题、错题本、Excel 导入导出
2. 导入题库：项目根的 `题库导入模板.xlsx` 在 quiz-app 仓库里，或用工具里的「下载模板」
3. 满意后 `npx -y @tauri-apps/cli@2 build` 重新出包

## 代码审查结论（用户要求全项目 bug 排查）

**发现 2 个 P0 阻断性问题，都是 `window.prompt` / `window.confirm` 在 Tauri 桌面端不可靠导致的。** 靠读代码发现不了，是查 Tauri 已知问题（官方 issue #4783 + 实测案例）坐实的。**单测 85 项全绿也照样漏**——这类问题只能在真机上走「取消路径」才能暴露。

| # | 级别 | 位置（web/index.html） | 问题 |
|---|---|---|---|
| 1 | **P0** | 3961–3980（`onModeClick`，9 处 `window.prompt`） | 刷题四种模式的入口**全靠 prompt 拿参数**。Tauri 未实现原生 prompt（issue #4783），桌面端最常见是直接返回 null → `onModeClick` 提前 return → **点了没反应，题根本开不了**，控制台还不报错 |
| 2 | **P0** | 4197 / 4207 / 4391 / 4432（`window.confirm`） | Tauri v2 + WebView2 桌面实测 confirm 是 **no-op 直接返回 true** → 「清空全部 N 道题」「清空错题本」「清空记录」「删除题目」**无任何确认直接执行**。静默数据丢失隐患 |
| 3 | P1 | 4046–4053（`resumeProgress`） | 恢复进度时 `list` 按题目存在性过滤，但 `answers` 用 `slice(0, list.length)` **按位置切** → 中间删过题后答案整体错位，对错题 |
| 4 | P1 | 4036（`saveProgress`） | `session.elapsed` 只在计时器回调里更新，而计时器**只在 maxTime 存在时启动** → 不限时模式下 elapsed 恒为 0，存档丢已用时间。应现场算 `Date.now()-startTime` |
| 5 | P1 | 4199 / 4210（删题、清空题型） | 只清了 `wrong`，**没同步活跃中的 `session`**（闭包里持有旧对象引用）→ 交卷时 `syncWrongOnSubmit` 会把已删题的 id 写进错题本，`viewWrong` 过滤掉不显示但数据永久残留 |
| 6 | P2 | 4250（`renderForm`） | 弹窗节点 `document.body.appendChild(box)`，**cleanup 不移除** → 切走工具后 DOM + 监听器泄漏 |
| 7 | P2 | 4298–4306（`loadXlsx`） | `_p` 缓存 rejected promise，**失败一次永久失败**，无法重试。catch 里应清空 `_p` |
| 8 | P2 | `saveData()` | 一次保存触发 3 次 `AppStore.set` = 3 次全量 stringify + 3 次全量写盘。题库入库后被放大 3 倍 → 该做存档拆分了 |
| 9 | P2 | `importExcel` | 每道题调一次 `nextQid()`（全量扫描）→ O(n²)；全空行也计入失败数 |
| 10 | P2 | `QuizCore.normalizeQuestion` | 不校验 choice 答案 ∈ {A,B,C,D}（Java 也不校验）→ Excel 填 E 会静默入库且永远判错 |
| 11 | P3 | `esc2`/`viewResult` 的 O(n²) find/判断题「正确答案 对」文案不一致/`bankSearch` 空列表时失焦 | 小问题 |

**没发现问题的部分**：QuizCore 核心规则（85 断言覆盖）、AppStore 写盘串行化（31 断言覆盖）、计时器与 keydown 的清理、blobToBase64 分块。并发维度：单实例插件已解决双实例互踩（待编译生效）。

**方法论沉淀**：已写入 skill `tauri2-desktop-wrap` 第 11 节。**修复方向**：统一自绘模态框 + Promise 包装成 `askConfirm`/`askInput`，浏览器和桌面同一套代码，不要按平台分流。修完后必须专门走「取消」路径验证。

### 修复完成（提交 `99b0282`，+235/-54）

P0 ×2 已修：
- 新增**顶层通用对话框设施**：`uiOverlay()`（全局只留一个遮罩，`__modalEl` 槽位）/ `uiCloseModal()` / `askConfirm()` / `askForm()`（支持 text/number/select/checks 四种字段，Promise 化）。放在 saveBlob 之后、工具注册表之前，是全局工具函数。
- `onModeClick` 改成 `async`，四种模式全部走 `askForm`：order/random 一个分类下拉；exam 题量+限时；custom 分类+题型勾选+题量+随机+限时。`types` 不勾 = 空数组 = QuizCore 里不过滤（等于全部）。
- 4 处 `window.confirm` 全部换 `askConfirm`（删题/清空题型/清空错题本/清空记录），mermaid 的清空源码也顺手换了（`.then()` 形式）。
- **全文件已无真实的 `window.prompt(` / `window.confirm(` 调用**（只剩注释）。

P1 ×3 已修：
- `resumeProgress`：按 id 配对成 `{q, a}` 再过滤已删题，答案不再错位；删掉了 `while(push)` 补齐。
- `saveProgress`：elapsed 现场算 `Date.now()-startTime`，不限时模式不再丢时间。
- 新增 `removeFromSession(ids)`：删题/清空题型时同步从活跃会话移除，交卷不会再把已删题 id 写进错题本。

P2 已修 4 个：
- `renderForm` 挂到 `__modalEl` 槽位，`cleanup()` 里 `uiCloseModal()` → 弹窗泄漏解决。
- `loadXlsx._p` 失败后清空，可重试。
- `saveData()` 改用 **`AppStore.setMany`**（新增的批量接口，合并成一次写盘），3 次全量写盘 → 1 次。
- `importExcel`：选择题答案校验 A-D。

**修复过程中自己又埋了一个 bug，被测试当场抓住**：校验选择题答案时写了 `'ABCD'.indexOf(answer) < 0`，字符串的 indexOf 是**子串匹配**，`'AB'` 也能通过。改成数组 `['A','B','C','D'].indexOf()`。这正是断言套件存在的意义。

最终验证：**quizcore 87/0，appstore 35/0**，`index.html` 217 KB / 4848 行。

**仍未验证**：UI 真机点按（尤其 askConfirm/askForm 的弹出与「取消」路径）。这必须人工做。
