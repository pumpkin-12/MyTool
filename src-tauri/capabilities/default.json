{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口权限。自定义命令（store_read / store_write / save_file）不受 capability 管辖，core:default 即可。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
